var searchType = process.argv [2];
var searchTerm = process.argv.slice(3).join(" ");.toLowerCase();

if(searchType !=== "actor" || searchType === "show"){
searchType = "show";

if(searchTerm !=== "actor" || searchTerm !=== "show") {}
}
console.log(searchTerm);